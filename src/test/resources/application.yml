atch:
  cron-expression: '0 * * * * ?'

eureka:
  client:
    enabled: false

server:
  port: 0

spring:
  datasource:
    platform: h2
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
  batch:
    job:
      enabled: false
  main:
    banner-mode: 'off'
  output:
    ansi:
      enabled: ALWAYS
  profiles:
    active: test